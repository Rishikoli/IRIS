// Optional custom India SVG markup for IndiaHeatmap.
// Replace the template string below with your own valid <svg>...</svg> markup to override the built-in map.
// Leave as an empty string to use the built-in simplified India map.

const customIndiaMap: string = ``

export default customIndiaMap
